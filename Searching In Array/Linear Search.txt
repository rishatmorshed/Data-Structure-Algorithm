#include<stdio.h>
#include<conio.h>
#define MAX_SIZE 10
int main()
{
	int A[MAX_SIZE], size, i, position, num;

	printf("Enter the size of the Array: ");
	scanf_s("%d", &size);

	printf("Enter the elements of the Array:\n");
	for (i = 0; i < size; i++)
	{
		scanf_s("%d", &A[i]);
	}

	printf("Enter the Number you want to Search: ");
	scanf_s("%d", &num);

		for (i = 0; i < size; i++)
		{
			if (A[i] == num)
			{
				i = i + 1;
			}

		}
		printf("After search from the Array:\n %d Found at position %d: ", num, i);

		
		
		_getch();
	}



